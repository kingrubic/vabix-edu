/**
 * Visual vehicle adapter. Interaction IDs, camera, and tire scenario live outside
 * this file so a licensed GLB (mapped in mesh-registry.ts) can replace the mesh later.
 * Current mesh is the original MyBizCar procedural sedan — not a Mercedes-Benz E300.
 */
export { VehicleBody as VehicleModel } from "./VehicleBody";
